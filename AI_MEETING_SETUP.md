# 미팅 기록일지 - AI 녹음/요약 기능 설정 방법

"미팅 기록" 메뉴 자체(수기로 회의 내용 적기)는 다른 파일들처럼 그대로 GitHub에 올리기만 하면 바로 됩니다.
그런데 그 안의 **"AI 녹음 시작"** 버튼(녹음 → 자동 전사 → 자동 요약)은 OpenAI라는 외부 AI 서비스를 호출해야 해서,
아래 두 가지를 한 번만 준비해주셔야 동작해요. 둘 다 마우스클릭만으로 되고 터미널은 필요 없습니다.

- **1단계: OpenAI API 키 발급** (유료 - 사용한 만큼만 과금되는 종량제)
- **2단계: Supabase에 "정리 담당" 함수 하나 배포** (그 함수 안에 위 키를 안전하게 보관)

키를 GitHub 코드에 직접 넣지 않고 Supabase에 따로 보관하는 이유는, GitHub 저장소 코드는(설정에 따라) 사람들이 볼 수도
있는데, 거기에 키를 그대로 넣으면 다른 사람이 그 키로 무제한 사용해서 요금이 나올 수 있기 때문이에요. Supabase
쪽에 "비밀 보관함(Secret)"으로 넣어두면 브라우저나 코드에는 절대 노출되지 않습니다.

---

## 1단계: OpenAI API 키 발급받기

1. https://platform.openai.com 접속 (chatgpt.com이 아니라 "platform" 쪽입니다) → 회원가입/로그인 (구글 계정으로도 가능)
2. 왼쪽 메뉴에서 **Billing(결제)** 들어가서 카드 등록 → **월 사용 한도(budget limit)** 를 걸어두세요 (예: 월 1~2만원).
   이 기능은 사용량이 아주 적어서(아래 "예상 비용" 참고) 한도를 낮게 잡아도 충분합니다.
3. 왼쪽 메뉴에서 **API keys** 클릭 → **Create new secret key** 클릭
4. 이름은 아무거나(예: gym-ops-meeting) 적고 생성 → 화면에 뜬 키(sk-로 시작하는 긴 문자열)를 **바로 복사**해두세요.
   ⚠️ 이 키는 한 번만 보여주고 다시는 못 봅니다. 메모장 등에 잠깐 붙여넣어 두세요.

---

## 2단계: Supabase에 "정리 담당" 함수 배포하기

1. Supabase 대시보드(프로젝트 열기) → 왼쪽 메뉴 **Edge Functions** 클릭
2. **Deploy a new function** → **Via Editor**(코드 직접 붙여넣기 방식) 선택
3. 함수 이름: `transcribe-summarize` (반드시 이 이름으로 — 코드에서 이 이름으로 호출하게 되어 있어요)
4. 에디터에 기존 예시 코드가 있으면 전부 지우고, 제가 드린 `edge-function-transcribe-summarize.ts` 파일의 내용을 그대로 붙여넣기
5. **Deploy function** 클릭 (10~30초 정도면 배포 완료)
6. 같은 Edge Functions 메뉴에서 **Secrets**(또는 "Manage secrets") 들어가서:
   - Key: `OPENAI_API_KEY`
   - Value: 1단계에서 복사해둔 `sk-...` 키
   - 저장

이게 끝입니다. 함수를 다시 배포할 필요 없이 바로 적용돼요.

---

## 그 다음: 평소처럼 코드/DB 반영

1. `migration_29_meeting_logs.sql`을 Supabase **SQL Editor**에서 실행 (미팅 기록을 저장할 표를 만듦)
2. 아래 파일들을 GitHub에 올려서 재배포 (기존 파일 덮어쓰기 + `meetings.html`은 새 파일로 추가)
   - `meetings.html` (신규)
   - `data.js`
   - `dashboard.html`, `home.html`, `members.html`, `sales.html`, `expiry.html`, `pt.html`, `tasks.html`, `products.html`, `staff.html` (상단 메뉴에 "미팅 기록" 링크만 추가된 것)

---

## 사용해보기

1. 메뉴에서 "미팅 기록" 클릭
2. "AI 녹음 시작" 클릭 → 브라우저가 마이크 권한을 물어보면 허용
3. 회의 진행 → 끝나면 "녹음 중지" 클릭
4. 몇 초~1분 정도 기다리면 "AI 요약"과 "전체 대화 전사" 칸이 자동으로 채워짐 (내용은 저장 전에 자유롭게 고칠 수 있어요)
5. 제목/참석자 적고 "저장"

만약 "AI 정리에 실패했습니다" 같은 에러가 뜨면, 위 1~2단계가 제대로 됐는지(특히 Secret 이름이 정확히
`OPENAI_API_KEY`인지, 함수 이름이 정확히 `transcribe-summarize`인지) 다시 확인해주세요.

---

## 예상 비용 (참고용)

- 음성 인식(전사): 분당 약 $0.006 (원화로 약 8~9원/분) — 예를 들어 30분 회의면 약 250~300원
- 요약(텍스트 정리): 회의 1건당 몇 원 수준으로 매우 저렴
- 즉, 회의 1건(30분 기준) 대략 300~400원 정도로 예상되고, 한 달에 회의를 자주 해도 몇천 원~1만원대에 그칠 가능성이 높습니다.
  (다만 OpenAI 가격 정책은 바뀔 수 있으니, Billing 페이지에서 실제 사용 내역을 가끔 확인하시는 걸 추천드려요.)
